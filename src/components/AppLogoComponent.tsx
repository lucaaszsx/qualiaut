export default function AppLogo({ className = '' }: { className: string }) {
    return (
        <img
            src=""
            alt="Logotipo do projeto Qualiaut"
            className={className} />
    );
}